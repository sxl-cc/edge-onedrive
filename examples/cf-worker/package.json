{
  "name": "cf-worker",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "check": "tsc --noEmit",
    "d": "pnpm -w build && wrangler deploy",
    "d:dry-run": "pnpm -w build && wrangler deploy --dry-run",
    "dev": "wrangler dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.2",
  "dependencies": {
    "api": "workspace:*"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260519.1",
    "wrangler": "^4.71.0"
  }
}
